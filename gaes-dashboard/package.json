{
  "name": "gaes-dashboard",
  "version": "1.0.0",
  "description": "GAES SEO Dashboard — React + Node/Express",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
